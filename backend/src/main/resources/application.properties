spring.application.name=backend
server.port=${PORT:8080}

# Database Configuration (Supabase Transaction Pooler - IPv4 compatible)
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.trfcigracylzxrsqqmtp
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration (Custom Property)
app.jwtSecret=DindigulPublicComplaintPortalSecretKeyForJWTAuth2026!@#$
app.jwtExpirationMs=86400000

# CORS Configuration
app.cors.allowedOrigins=*
